# Defaults for all slide decks in the slides/ directory.
# Individual decks only need title + subtitle in their YAML.
format:
  revealjs:
    theme:
      - simple
      - ../theme.scss
    slide-number: true
    code-copy: true
    transition: fade
    chalkboard: true
    footer: "STAT 386 — Fall 2026"
execute:
  echo: true
  warning: false
  message: false